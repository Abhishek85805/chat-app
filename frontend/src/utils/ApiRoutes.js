const host = "http://localhost:3000";

const registerRoute = `${host}/api/user/register`;
const loginRoute = `${host}/api/user/login`;
const setAvatar = `${host}/api/user/set-avatar`;

export {
    registerRoute,
    loginRoute,
    setAvatar
}